# 架构评审 · 结项与开工须知

> 评审对象：`DESIGN.md`（印尼股市营销文案变体系统 · 总体设计 v3）
> 最近复审：2026-07-01（对 v3 最新版）
> 面向对象：**接手实现的工程师 / Claude** —— 先读本文件再动手。

---

## 一句话结论

**一期设计已达生产级门槛，无设计层阻断项，可直接进入实现。**
全部评审缺陷（P0-1~3 / P1-1~4 / P2-1~5 / R-1~3）已在设计层关闭，关闭记录见 `DESIGN.md` §12。
**仅剩 1 个非技术项（A-1）阻断"对外正式投放"，但不阻断一期开发实现。**

---

## 开工前必读的三条边界

1. **可以动手写代码。** 一期范围与"一期必含"清单见 `DESIGN.md` §8。设计已定稿，按 §3 各模块（M1/M3/M5/M6）+ §4 反馈闭环 + §6 成本护栏 实现即可。

2. **对外投放是一道闸，开发不是。**（见下方 A-1）
   —— 你可以把整条链路开发、联调、内部试用跑通；但**面向真实投放对外发内容之前**，必须先拿到需求方的法务书面回置。这不是你能在代码里解决的，遇到就停下上报，别自己放行。

3. **一期必须内建、不能省的合规/安全底线**（来自评审，已写进 §8）：
   - 禁词硬拦截 + Haiku 语义合规兜底（§3 M6-3）
   - 抓取内容抗注入：不可信数据隔离/转义（§3 M3/M5）
   - M6 重写迭代上限 2–3 次 + 超限降级（§3 M6-1）
   - golden 集接 CI 门禁：prompt/模型变更必过回归才上线（§8）
   - 密钥托管 + 抓取源 ToS 确认（§7）

---

## 唯一未决项（阻断"对外投放"，不阻断"开发"）

### A-1　法务书面回置 🔴 ⏳ 未闭环
- **性质**：唯一非设计/非技术可消解项。系统在 OJK 监管市场批量生产荐股营销内容，技术侧只能做到"禁词 + Haiku 语义"拦截，**消解不了"业务模式本身是否合规"的法律责任**。
- **要求**：需求方找真人法务确认，并**书面知悉后方可正式对外投放**。
- **对开发的影响**：**无**。开发/联调/内部试用可正常推进；仅"真实对外发布"这一步等此签字。

---

## 一条建议（非阻断，实现时顺手做）

### R-4　bandit 合规惩罚项的 λ 需标定 ⚠️
- **位置**：`DESIGN.md` §4 —— 奖励 `= 采用信号 − λ·合规命中`。
- **问题**：λ 太大→文案过度保守变寡淡；太小→合规惩罚失效。
- **建议**：把 λ 挂到 golden 集标定，目标为"合规命中率↓ 且 采用率不明显下滑"，别拍脑袋定值。实现反馈闭环时一并处理。

---

## 汇总

| 编号 | 项 | 状态 | 阻断范围 | 谁来做 |
|---|---|---|---|---|
| P0/P1/P2/R-1~3 | 全部评审缺陷 | ✅ 设计层已关闭 | — | 已并入 DESIGN.md |
| A-1 | 法务书面回置 | ⏳ 未闭环 | 仅"对外投放" | 需求方（法务） |
| R-4 | λ 标定 | 🔧 待实现时做 | 优化 | 实现方 |

> **给接手者**：按 `DESIGN.md` 开工即可。唯一要守住的红线——**没拿到 A-1 法务签字前，不做真实对外投放**，其余全部放行。
