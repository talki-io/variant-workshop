# 开工交接（给接手实现的 Claude / 工程师）

> 本文件是实现阶段的入口。**先读这份，再读 `DESIGN.md`，然后动手。**
> 需求与架构已经过两轮独立评审收敛，设计已定稿，**无设计层阻断项，可直接开工**。

---

## 1. 三个文件的关系（按顺序读）

1. **`HANDOFF.md`（本文件）** —— 开工须知、从哪起步、红线、哪些别推翻。
2. **`DESIGN.md`（v3，唯一权威设计）** —— 完整方案。§3 模块、§4 反馈闭环、§6 成本护栏、§8 分期、§12 缺陷处置对照。
3. **`ARCH-REVIEW-DEFECTS.md`** —— 评审结项记录，证明设计已达生产级门槛。仅供追溯，不需再改。

---

## 2. 一句话项目定位（别跑偏）

**内部"出稿加速器",不是"爆款自动机"。** 输入素材员的历史爆款（40–50 条、多账号）+ 自动采集的印尼股市新闻,产出**有真人味、贴热点、过合规底线**的文案变体草稿,供素材员选改。人始终在选稿与合规环节。

---

## 3. 🔴 唯一红线（写代码解决不了，遇到就停）

**A-1：正式对外投放前，必须先拿到需求方的法务书面确认。**
系统在 OJK 监管市场批量生产荐股营销内容,技术侧只能做到"禁词 + Haiku 语义"拦截,**消解不了业务模式本身的法律责任**。
→ 开发、联调、内部试用：全部放行。
→ 真实对外发布：等法务签字,别自己放行,遇到上报。

---

## 4. 建议的开工顺序（第一期）

按依赖关系,**从地基往上建**：

1. **离线校准层（最先，是全系统地基）** ——`DESIGN.md` §3 离线校准层
   拆 40–50 条爆款 → ① 模式库 + ② **多调性**语感指纹（按账号聚类，绝不平均）+ ③ 风格参考向量 + ④ 去AI味判别器 few-shot + ⑤ golden 评测集。
2. **数据底座** —— Postgres + pgvector；建表：新闻/热点卡/模式库/风格向量/变体/权重表/token/埋点。
3. **采集 + 清洗（M1 → M3）** —— RSS/搜索API 优先，Playwright 兜 JS 站；M3 含**抗注入**（抓取内容当不可信数据）。
4. **生成 + 评审（M5 → M6）** —— 对话先指定目标账号/调性；M6 三层合规（禁词+Haiku语义+软提示）+ 重写迭代上限 2–3 次 + 抗注入。
5. **前端 + 鉴权** —— Ant Design + Ant Design X（对话）+ Ant Design Charts；双角色。
6. **成本护栏（一期必含）** —— token 记录 + 看板 + 配额/限流。
7. **反馈埋点 + bandit** —— 隐式行为埋点；ε-greedy 先粗（hook 级）后细；奖励含合规惩罚项（λ 挂 golden 集标定，R-4）。

---

## 5. 一期必内建、不能省的底线（来自评审 §8）

- 禁词硬拦截 + **Haiku 语义合规兜底**（§3 M6-3）
- 抓取内容抗注入：不可信数据隔离/转义（§3 M3/M5）
- M6 重写**迭代上限 2–3 次 + 超限降级**（§3 M6-1）
- **golden 集接 CI 门禁**：prompt/模型变更必过回归才上线（§8）
- 密钥托管 + 轮换；抓取源 ToS/robots 确认（§7）
- 成本配额/限流（§6）

---

## 6. 已拍板、不要再推翻的决策（省得重新论证）

| 决策 | 结论 |
|---|---|
| 技术路线 | 提示工程 + 检索 + 规则，**零模型训练**（数据仅 40–50 条） |
| 反馈机制 | **隐式行为信号**为主 + bandit 探索；显式评价纯可选。不重度依赖用户打分 |
| 语感指纹 | 多账号 → **按调性分桶**独立建，绝不平均 |
| 质量指标 | **变体采用率**（导出/采用比例），不用爆款率 |
| 前端 | Ant Design 全家桶，**用现成组件不手写** |
| 采集工具 | **Playwright**（非 agent-browser）；RSS/API 优先 |
| 合规 | 禁词 + Haiku 语义 + 软提示 + 人工把关；快线保留一键确认 |
| 编排 | 一期轻量 async，预留 DAG 抽象；扩展再上 LangGraph |
| 模型 | Haiku（清洗/分类/语义合规）；Opus 4.8 或 Sonnet（生成/评审/去AI味）；快线走 Sonnet |

---

## 7. 环境备注

- 工作目录：`/home/tel/aiProject/ai-agent-imitator`（受信任目录，文件操作无需额外授权）。
- 目前仓库尚无实现代码,只有三份设计文档。**从零搭工程骨架。**
- 全局文件操作规则见 `~/.claude/CLAUDE.md`（删除命令规范、系统文件保护等）。

---

## 8. 开工第一步建议

先跑通"离线校准层"里最小的一环——**拿 3–5 条爆款试拆一份模式库 + 语感指纹**,验证提示词能稳定产出结构化结果,再规模化到 40–50 条。地基对了,后面才稳。
